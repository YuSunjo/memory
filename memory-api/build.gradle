plugins {
    id 'java'
}

bootJar {enabled = true}
jar {enabled = false}

dependencies {

    implementation project(':memory-common')
    implementation project(':memory-domain')
}

tasks.named('test') {
    useJUnitPlatform()
}